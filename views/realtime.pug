extends layout

block content
  h1 Realtime
  p(id='result')
  button(onclick="takePicture()" disabled="disabled") Take Picture!
  input(type='file' onchange="uploadPicture()" id='upload' disabled="disabled")
  button(onclick="examplePicture()" disabled="disabled") Use Example!
  button(onclick="redirectNonRealtime()") Leave realtime!
  p Leave realtime mode to access webcam capture, picture upload and example mode
  br
  script(async defer src='/javascripts/bodypix_realtime.js')